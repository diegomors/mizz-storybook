
  # Workshop UI - Make

  This is a code bundle for Workshop UI - Make. The original project is available at https://www.figma.com/design/SG2OWkyUJz0rTEPX96An04/Workshop-UI---Make.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  